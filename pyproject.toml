[project]
name = "calwbot"
version = "0.1.0"
description = "Retail AI Assistant (OpenClaw + tool-calling simulation)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "google-generativeai>=0.8.0",
]

[tool.uv]
dev-dependencies = []
